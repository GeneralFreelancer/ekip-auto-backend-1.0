import setPassword from './set'
// import confirmPassword from './confirm'
// import recoverPassword from './recover'
import renewPassword from './renew'
import changePassword from './change'

export default {
    setPassword,
    // confirmPassword,
    // recoverPassword,
    renewPassword,
    changePassword,
}
